@model List<Write.io.Models.Blog>

<div class="BlogGrid">
    @foreach (var blog in Model)
    {
        var headerimage = "";
        if (blog.BlogHeaderURL != null)
        {
            headerimage = "style=background-image:url(" + @blog.BlogHeaderURL + ")";
        }
        <a class="BlogCardLink" href="#">
            <div class="BlogCard">
                <div class="BlogCardHeader" @headerimage>
                    <h2>@blog.Title</h2>
                </div>
                <div class="BlogCardBody">
                    <p>@blog.Body</p>
                    @Html.ActionLink("Edit", "EditBlog", "User", new { id = blog.Id }, new { @class = "btn btn-default" })
                    @Html.ActionLink("Remove", "RemoveBlog", "User", new { id = blog.Id }, new { @class = "btn btn-info" })
                </div>
            </div>
        </a>
    }
</div>